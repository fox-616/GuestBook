@model GuestBook.Models.ReBook

@{
    Layout = null;
    // ViewData["Title"] = "Create";
    // var Manager = Context.Session.GetString("Manager");
    // if (Manager != null)
    //     Layout = "_Layout";
}

<div class="row">
    <div class="col">
        @* 3.5.10 將RePostBooks\Create View中<form>加上id *@
        <form asp-action="Create" id="reForm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Description" class="control-label"></label>
                <textarea asp-for="Description" class="form-control" rows="4"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Author" class="control-label"></label>
                <input asp-for="Author" class="form-control" />
                <span asp-validation-for="Author" class="text-danger"></span>
            </div>
            @*  <div class="form-group">
            <label asp-for="TimeStamp" class="control-label"></label>
            <input asp-for="TimeStamp" class="form-control" />
            <span asp-validation-for="TimeStamp" class="text-danger"></span>
            </div> *@
            @*  <div class="form-group">
            <label asp-for="GId" class="control-label"></label>
            <select asp-for="GId" class ="form-control" asp-items="ViewBag.GId"></select>
            </div> *@
            <input type="hidden" name="GId" value="@ViewBag.GId" />
            @* <div class="form-group mt-3 text-end">
                <input type="submit" value="確定回覆" class="btn btn-warning" />
            </div> *@
        </form>
    </div>
</div>


@* @section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
} *@
